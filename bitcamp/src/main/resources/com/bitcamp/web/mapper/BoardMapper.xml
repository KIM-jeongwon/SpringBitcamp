<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bitcamp.web.mapperImpl.BoardMapperImpl">
 	<select id="selectBoard" resultType="board" parameterType="cmd">
select A2.* from(
        select rownum rn, A.* 
            from(select 
                    bbs_seq as bbsSeq,
                    title,
                    content,
                    regdate,
                    id 
                        from board 
                            order by bbsSeq asc
               ) A ) A2
           where rn between #{paging.startRow} and #{paging.endRow}
  	</select>
   		<select id="selectCount" resultType="board" parameterType="cmd">
  		SELECT bbs_seq AS bbsSeq,title,content,regdate,id FROM board WHERE bbs_seq LIKE  #{board.bbsSeq}
  	</select> 
   	<select id="boardCount" resultType="int">
  		SELECT count(*) FROM board
  	</select> 
</mapper>
<!-- SELECT bbs_seq AS bbsSeq,title,content,regdate,id FROM board -->

